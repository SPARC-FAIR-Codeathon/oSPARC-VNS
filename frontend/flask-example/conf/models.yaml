
model_1:
  index: 1
  author: 'Zaher Joukhadar <zaher.joukhadar@unimelb.edu.au>'
  # tuning/processed parameters 
  patch_pixel_size: 0.03 
  patch_size: 512 # 512, needs to match 
  data_sets_to_use_in_modelling: keast_sparc
  # tuning/modelled parameters 
  parallel_processing: true
  loss_type: dice_all_equal
  epochs: 10000
  visualize_activations: false
  save_augmentation_output: false
  random_seed: 2020
  train_set_size: 0.7

# this needs to get updated each time a new model is trained
